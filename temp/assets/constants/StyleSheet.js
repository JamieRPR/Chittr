'use strict';

var React = require('react-native');

var {
  StyleSheet,
} = React;

module.exports = StyleSheet.create({
    userInputField: {
        flexDirection:'row', 
        alignItems:'center',
        backgroundColor: 'red'
    }
});